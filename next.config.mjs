/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "upload.wikimedia.org",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
